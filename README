* Files
The basic distribution contain the following files:
	README
	Makefile Makefile.config.model
	Changes
	manager.idl    bdd.idl    rdd.idl    idd.idl 
	manager.mli    bdd.mli    rdd.mli    idd.mli    mtbdd.mli  
	manager.ml     bdd.ml     rdd.ml     idd.ml     mtbdd.ml   
	session.ml example.ml

	cudd_caml.c manager_caml.c bdd_caml.c rdd_caml.c idd_caml.c
	cudd_caml.h sedscript_c sedscript_caml macros.m4
	texinfo.tex mlcuddidl.texi
	mlcuddidl.dvi mlcuddidl.info ./html/*.html

* What's needed :
- An ANSI C compiler (gcc is the only tested compiler)
- GNU MAKE
- The CUDD BDDs library (http://vlsi.colorado.edu/software.html)
- The CUDDAUX extension library (same location as this package)
- OBJECTIVE CAML 3.0 or higher (http://caml.inria.fr)
- CAMLIDL 1.05 (http://caml.inria.fr/camlidl)

- GREP, SED, M4 if you want to regenerate files from .idl files

* This code provides an interface to user's functions of the CUDD
library. It generates five ML modules: Manager, Bdd, Rdd, Idd, Mtbdd.

* To build,
Set Makefile.config from Makefile.config.model model.

IMPORTANT REMARK: The C compilation flags in XCFLAGS (processor, alignement)
should be the same than those with which CUDD and CUDDAUX are compiled.

make rebuild: requested only if download from subvsersion directory.
	      regenerate C and OCaml files from .idl files
              (require m4, sed)
make all: build the bytecode and native versions of the interface
make install: installs the two versions of the interface
make cuddrun: builds a OCaml bytecode interpreter containing the
              CUDD and CUDDAUX libraries
make cuddtop: builds a OCaml toplevel containing the interface
make example: build bytecode version of example.ml
make example.opt: build native-code version of example.ml
make clean: normal clean
make mostylclean: delete also generated files (from .idl files)
make rebuild: regenerate C and OCaml files from .idl files
              (require m4, sed)
make mlcuddidl.dvi:

You can also look at session.ml.

* Documentation: DVI (mlcuddidl.dvi), INFO (mlcuddidl.info), HTML (HTML/*)
